import { h, Component } from 'preact'

class App extends Component {
    render (props) {
        return (
            <div class='container' id='container'>
                BOILERPLATE
            </div>
        )
    }
}

export default App;
